/*
 * Tests for organization-specific operations outside of basic CRUD operations.
 */
context.skip('Organization operations', function() {
  it('has the add button visible when returning an empty list of orgs', function() {})
  it('has proper field validation')
  it('has a delete button on detail views')
  it('can navigate and/or sort pages')
})
