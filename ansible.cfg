[defaults]
host_key_checking=False
roles_path = playbooks/roles
stdout_callback = yaml
timeout = 60

[ssh_connection]
control_path = %(directory)s/%%h-%%r
ssh_args = -o ServerAliveInterval=30 -o ControlMaster=no -o ControlPersist=60s -o ForwardAgent=yes -o StrictHostKeyChecking=no
retries = 5
