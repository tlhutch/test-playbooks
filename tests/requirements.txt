# This pulls in all the libraries needed to run Selenium tests
# on Mozilla WebQA projects

selenium
py
pytest
pytest-cov
pytest-mozwebqa
# pytest-jira # Don't install until we fix the pytest-jira plugin
UnittestZero
requests
PyYAML
jsonschema
pip
inflect
ansible

# Support running celery flower for job queue visualization
flower
