---
$schema: http://json-schema.org/draft-04/schema#
id: '/api/v1/hosts/\d+/fact_versions/'
type: object
additionalProperties: false
required: [results]
properties:
  results:
    type: array
    minItems: 0
    uniqueItems: true
    items:
        $ref: '#/definitions/fact_version'
definitions:
  !include '../definitions/*.yml'
