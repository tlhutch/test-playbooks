---
summary_fields_credential:
    additionalProperties: false
    properties:
      cloud: {type: boolean}
      description: {type: string}
      kind: {$ref: '#/definitions/enum_kind'}
      name: {type: string}
    required: [name, description, kind, cloud]
    type: object
