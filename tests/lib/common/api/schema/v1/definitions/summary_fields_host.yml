---
summary_fields_host:
    additionalProperties: false
    properties:
      description: {type: string}
      has_active_failures: {type: boolean}
      has_inventory_sources: {type: boolean}
      id: {$ref: '#/definitions/id_or_null' }
      name: {type: string}
    required: [ description, has_active_failures, has_inventory_sources, id, name, ]
    type: object
