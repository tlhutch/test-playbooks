summary_fields_permissions:
  additionalProperties: false
  properties:
    admin_role: {type: boolean}
    read_role: {type: boolean}
    member_role: {type: boolean}
    scm_update_role: {type: boolean}
    owner_role: {type: boolean}
    user_role: {type: boolean}
    execute_role: {type: boolean}
    adhoc_role: {type: boolean}
    update_role: {type: boolean}
    use_role: {type: boolean}
  type: object
