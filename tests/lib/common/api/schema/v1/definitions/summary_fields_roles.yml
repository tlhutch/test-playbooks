summary_fields_role:
  additionalProperties: false
  properties:
    description: {type: string}
    id: {$ref: '#/definitions/id'}
    name: {type: string}
    url: {format: uri, type: string}
  required: [url, description, id, name]
  type: object
