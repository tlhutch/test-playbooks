---
summary_fields_last_job:
    additionalProperties: false
    properties:
      id: {$ref: '#/definitions/id'}
      name: {type: string}
      description: {type: string}
      failed: {type: boolean}
      finished: {format: date-time, type: string}
      job_template_id: {$ref: '#/definitions/id'}
      job_template_name: {type: string}
      status: {$ref: '#/definitions/enum_source_status'}
    # FIXME: required: [id, name, description, status, failed, job_template_id, job_template_name]
    required: [name, description, status, failed, job_template_id, job_template_name]
    type: object
