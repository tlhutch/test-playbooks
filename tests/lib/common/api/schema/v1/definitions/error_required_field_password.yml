---
error_required_field_password:
    type: array
    minItems: 1
    uniqueItems: True
    items:
        oneOf:
            - type: string
              pattern: '^Password required for new User.$'
            - type: string
              pattern: '^This field is required.$'
