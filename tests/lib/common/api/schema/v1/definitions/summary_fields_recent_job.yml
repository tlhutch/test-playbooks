summary_fields_recent_job:
  additionalProperties: false
  properties:
    finished:
      format: date-time
      type: [string, 'null']
    id: {type: number}
    status: {$ref: '#/definitions/enum_source_status'}
    name: {type: string}
  required: [id, finished, status]
  type: object
