---
$schema: 'http://json-schema.org/draft-04/schema#'
id: '/api/v1/'
type: object
additionalAttributes: false
required: [activity_stream, authtoken, config, credentials, dashboard, groups, hosts, inventory, inventory_sources, job_templates, jobs, me, organizations, projects, teams, users]
properties:
    activity_stream: { $ref: '#/definitions/uri' }
    authtoken: { $ref: '#/definitions/uri' }
    config: { $ref: '#/definitions/uri' }
    credentials: { $ref: '#/definitions/uri' }
    dashboard: { $ref: '#/definitions/uri' }
    groups: { $ref: '#/definitions/uri' }
    hosts: { $ref: '#/definitions/uri' }
    inventory: { $ref: '#/definitions/uri' }
    inventory_sources: { $ref: '#/definitions/uri' }
    job_templates: { $ref: '#/definitions/uri' }
    jobs: { $ref: '#/definitions/uri' }
    me: { $ref: '#/definitions/uri' }
    organizations: { $ref: '#/definitions/uri' }
    projects: { $ref: '#/definitions/uri' }
    teams: { $ref: '#/definitions/uri' }
    users: { $ref: '#/definitions/uri' }
definitions:
  !include 'definitions/*.yml'
