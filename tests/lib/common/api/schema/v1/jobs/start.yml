---
$schema: http://json-schema.org/draft-04/schema#
id: '/api/v1/jobs/\d+/start/'
type: object
additionalProperties: false
required: [can_start]
properties:
  can_start:
    type: boolean
  passwords_needed_to_start:
    $ref: '#/definitions/passwords_needed_to_start'
  vars_prompt_on_launch:
    type: boolean
definitions:
    !include '../definitions/*passwords_needed_to_start.yml'
