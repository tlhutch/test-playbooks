---
$schema: http://json-schema.org/draft-04/schema#
id: '/api/v1/me/'
type: object
additionalProperties: false
required: [count, next, previous, results]
properties:
  count: {minimum: 1, maximum: 1, type: number}
  next:
    type: 'null'
  previous:
    type: 'null'
  results:
    type: array
    minItems: 1
    maxItems: 1
    uniqueItems: true
    items:
        $ref: '#/definitions/user'
definitions:
  !include '../definitions/*.yml'
