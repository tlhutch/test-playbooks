---
summary_fields_project:
    type: object
    required: [name, description]
    # FIXME: required: [id, name, description]
    additionalProperties: false
    properties:
      id: {$ref: '#/definitions/id'}
      description: {type: string}
      name: {type: string}
      status:
        $ref: '#/definitions/enum_project_status'

