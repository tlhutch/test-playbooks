---
dashboard_common_core:
    additionalProperties: false
    properties: # {$ref: '#/definitions/dashboard_common_core_fields'}
      total: {minimum: 0, type: number}
      url: {format: uri, type: string}
    required: [url, total]
    type: object
