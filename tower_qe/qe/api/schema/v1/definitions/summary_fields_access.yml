summary_fields_access:
  additionalProperties: false
  properties:
    role:
      additionalProperties: false
      properties:
        description: {type: string}
        id: {$ref: '#/definitions/id'}
        name: {type: string}
        related: {type: object}
        resource_name: {type: string}
        resource_type: {type: string}
        team_id: {$ref: '#/definitions/id'}
        team_name: {type: string}
        user_capabilities:
           additionalProperties: false
           properties:
             unattach: {type: boolean}
           required: [unattach]
           type: object
      required: [description, id, name, user_capabilities]
    descendant_roles:
      items: {$ref: '#/definitions/enum_descendant_role'}
      minItems: 0
      type: array
      uniqueItems: true
  required: [role, descendant_roles]
  type: object
