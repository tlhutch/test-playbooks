---
fact_version:
  additionalProperties: false
  properties:
    related:
      additionalProperties: false
      properties:
        fact_view: {format: uri, type: string}
      required: [fact_view]
      type: object
    module: {type: string}
    timestamp: {format: date-time, type: string}
  required: [related, module, timestamp]
  type: object
