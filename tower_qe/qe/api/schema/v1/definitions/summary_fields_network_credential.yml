summary_fields_network_credential:
  additionalProperties: false
  properties:
    description: {type: string}
    id: {$ref: '#/definitions/id'}
    kind: {pattern: ^net$, type: string}
    name: {type: string}
  required: [id, name, description, kind]
  type: object
