---
$schema: 'http://json-schema.org/draft-04/schema#'
id: '/api/v1/ping/'
type: object
additionalAttributes: false
required: [instances, ha, version]
properties:
    instances:
      type: array
      items:
        type: string
    ha:
      type: boolean
    role:
        enum: [primary, secondary]
    version:
        $ref: '#/definitions/version_string'
definitions:
  !include 'definitions/*.yml'
