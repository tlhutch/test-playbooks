$schema: http://json-schema.org/draft-04/schema#
additionalAttributes: false
definitions: null
id: /api/v1/ping/
properties:
  active_node: {type: string}
  ha: {type: boolean}
  instances:
    items:
      additionalPropeties: false
      properties:
        heartbeat: {format: date-time, type: string}
        node: {type: string}
      type: object
    type: array
    minItems: 1
    uniqueItems: true
  version: {$ref: '#/definitions/version_string'}
required: [instances, ha, version, active_node]
type: object
definitions:
  !include 'definitions/*.yml'
