---
# If the desired image exists, the value of `skip_delete` determines
# whether to use the existing image, or to delete it before proceeding
skip_delete: True

# AWS variables
aws_url: 'https://ec2.amazonaws.com'
aws_access_key: ''
aws_secret_key: ''
ec2_region: 'us-east-1'
ec2_instance_type: 'm3.medium'
ec2_security_group: 'AWX' # sg-b05be0db
ec2_key_name: 'jenkins'
ec2_name_prefix: '{{ instance_name_prefix }}'
# ec2_images are defined in group_vars/all since they are needed by multiple roles
# ec2_images: []
